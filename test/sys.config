[{mysql_pool, [
    {pools, [
        {mypool, [
            {size, 20},
            {connection_options, [
                {host, "localhost"},
                {user, "root"},
                {password, "root"},
                {database, "mysql_otp_test"}
            ]}
        ]}
    ]}
]}].